---
layout: default
---

<% for block in @getCollection("html").findAllLive({relativeOutDirPath: @document.collection }, [ order: 1, title: 1 ] ).toJSON(): %>

  <a id="<%= block.basename %>"> <%= block.title || 'no title' %></a>
  <%- block.contentRenderedWithoutLayouts %>

<% end %>